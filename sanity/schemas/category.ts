import { title } from "process";

export default {
    type:"document",
    name:"category",
    title:"Categories",
    fields:[
        {
            type:"string",
            name:"name",
            title:"Category Name"
        }
    ]
}